public class RegularExpression {
    public boolean isMatch(String s, String p) {
        if (s.isEmpty() || p.isEmpty())
            return false;
        else {
            return true;
        }
    }
}
